# Python Chess API

A custom chess engine built from scratch in Python. It handles the complete ruleset of the game (validations, checks, en passant) and is structured to be connected to a web frontend via an API.

## Tech Stack
- Python 3.10+

## Setup
*(Instructions for running the API will be added once the web integration is complete.)*